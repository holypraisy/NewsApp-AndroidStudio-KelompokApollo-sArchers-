<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsAPI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@android:color/white</item>
        <!-- Jika Anda juga ingin mengubah warna toolbar, Anda bisa menggunakan item berikut -->
        <!-- <item name="android:colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NewsAPI" parent="Base.Theme.NewsAPI" />
</resources>
